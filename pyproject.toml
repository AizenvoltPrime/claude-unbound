[project]
name = "tcp-proxy-server"
version = "1.0.0"
description = "TCP Proxy Server"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.75.0",
    "litellm[proxy]",
    "python-dotenv>=1.2.1",
    "uvicorn>=0.31.1",
]

[tool.uv]
dev-dependencies = []
